Task: Phase 4 Task 3 - Refactor pre_run_hook.gd (Optional)
Commit: e0d1f9f

Changes:
- Added docstrings to 4 methods: _instrument_all(), _instrument_script(), _inject_trackers(), _get_indentation()
- No structural changes - code already follows GDScript style (tabs, snake_case, type annotations)
- Error messages reviewed: all are actionable with [gd-tools] prefix, include relevant paths and error details

Post-refactor: 4/4 pre_run_hook tests still pass (docstrings are comments only, no behavior change).
