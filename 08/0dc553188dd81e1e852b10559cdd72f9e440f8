Phase 2: Lint Command Output Standardization (FR-2)

Changes:
- src/gd_tools/lint_runner.py: Refactored format_lint_text() to print directly
  via shared output module (output.console, output.print_success,
  output.print_summary, output.print_info) instead of using Console().capture()
  and returning a string. Return type changed from str to None.
- src/gd_tools/cli.py: lint command now calls format_lint_text(result) directly
  without click.echo(). JSON mode unchanged (still uses click.echo).
- tests/unit/test_lint_runner.py: Updated 6 format_lint_text tests to use
  capsys for stdout capture. Added test_format_lint_text_prints_directly to
  verify None return type. Color tests now monkeypatch gd_tools.output.console.

Test Results:
- test_lint_runner.py: 31 passed (7 updated, 1 new)
- test_cli.py: 82 passed (no changes needed)
- Full unit suite: 708 passed

Coverage (lint_runner.py):
- Line: 96% (75 statements, 4 missed - run_lint file-skip warnings)
- Branch: 100% (18 branches, 0 missed)

Quality Gates:
- ruff check: All checks passed
- black --check: 3 files would be left unchanged
- Tests: 708 passed
- Coverage: lint_runner.py 96% line / 100% branch (above 80%/70% thresholds)

FR-2 Compliance:
- [x] file:line:col: rule: message [ERROR/WARN] format preserved
- [x] Shared helpers used for summary (print_summary) and clean state (print_success)
- [x] Console().capture() replaced with direct console.print()
- [x] JSON mode unchanged

Phase 2 Verification Report
