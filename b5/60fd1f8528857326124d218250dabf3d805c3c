Phase 2 Verification Checkpoint (b560fd1)

PHASE: Phase 2 - Orchestrator Module
STATUS: APPROVED by user

CHANGED FILES (since Phase 1 checkpoint efa756c):
- src/gd_tools/coverage/orchestrator.py (NEW, 275 lines, 4 functions)
- src/gd_tools/coverage/__init__.py (UPDATED, re-exports)
- tests/unit/test_orchestrator.py (NEW, 508 lines, 30 tests)
- conductor/tracks/coverage_cli_20260711/plan.md (UPDATED)

IMPLEMENTATION COMMITS:
- b4f7203: run_coverage_test() - 10 tests
- 90d9177: generate_coverage_report() - 7 tests
- bcd6762: merge_coverage_files() - 6 tests
- e98382b: show_coverage_summary() - 7 tests
- 6fa3535: __init__.py re-exports
- 721c9b7, 29dad57: plan.md updates

TEST RESULTS:
- 531/531 unit tests pass (30 new orchestrator + 501 existing)
- orchestrator.py: 100% coverage (71 statements, 0 missed)
- Overall project: 98.49% coverage

QUALITY GATES:
- ruff: All checks passed
- black: All files unchanged

FUNCTIONS IMPLEMENTED:
1. run_coverage_test(config, suite, test_name, junit_xml, no_exit_code, min_percent, timeout) -> TestResult
   - Generates plan, writes plan.json, runs tests with coverage=True,
     reads coverage data, generates report, applies threshold
   - Error precedence: TestFailureError > CoverageThresholdError
2. generate_coverage_report(config, format, output_dir) -> ReportResult
   - Reads existing plan+coverage, regenerates reports
   - Resolves format/output_dir: CLI flag > config > default
3. merge_coverage_files(files, output) -> CoverageData
   - Delegates to reporter.merge_coverage_data, writes JSON to output
   - Default output: .gd-tools/coverage/coverage.json
4. show_coverage_summary(config, min_percent) -> CoverageSummary
   - Reads plan+coverage, computes summary, prints Rich table
   - Raises CoverageThresholdError if line_rate*100 < min_percent
